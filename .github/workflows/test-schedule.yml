name: Schedule Test

on:
  workflow_dispatch:

  schedule:
    - cron: "*/15 * * * *"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Print test message
        run: echo "Schedule works!"

      - name: Resync marker
        run: echo "Scheduler resync test"

# resync schedule
